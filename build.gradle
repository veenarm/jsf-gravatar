apply plugin: "java"

subprojects {
  apply plugin : 'java'
  apply plugin: 'maven'
  
  repositories {
     mavenCentral()
     
     maven {
        url 'http://myfaces.zones.apache.org/dist/maven-repository'
     }
  }
}
